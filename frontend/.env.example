# Server-only URL used by Next.js to proxy /api requests to the backend.
# It is needed during `next build` and by the running server for SSR. Use the
# same value in both places; never prefix it with NEXT_PUBLIC_.
BACKEND_URL=http://localhost:8080
